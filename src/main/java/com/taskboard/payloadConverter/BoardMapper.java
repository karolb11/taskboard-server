package com.taskboard.payloadConverter;

import org.springframework.stereotype.Service;

@Service
public class BoardMapper {
    //public Function<Board, BoardWidgetResponse> boardToBoardWidgets =
    //        board -> new BoardWidgetResponse(board.getId(), board.getName(), board.getDescription());

}
